{
  "name": "movie-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Movie artwork, trailers and newsletter workspace",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --env-file-if-exists=.env server.mjs",
    "build": "node --check server.mjs",
    "lint": "node --check server.mjs api/index.mjs public/core.mjs public/app.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "firebase": "^12.19.0",
    "jszip": "3.10.1",
    "pdfjs-dist": "5.6.205"
  }
}
